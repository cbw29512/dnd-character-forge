import { RANDOM } from "../schema.js";
import { pick } from "./random.js";
const NORMAL_LEVELS=Object.freeze([4,8,12,16]),LEVELS_2014=Object.freeze([4,8,12,16,19]),EPIC_BOONS=Object.freeze(["boon-combat-prowess","boon-dimensional-travel","boon-fate","boon-irresistible-offense","boon-night-spirit","boon-truesight"]);
export function applyBarbarianAdvancement(character,data,selections={}){
  try{
    if(character.class.id!=="barbarian")return character;const next=structuredClone(character),choices=[];
    if(character.ruleset==="2014"){
      let grapplerTaken=false;for(const level of LEVELS_2014){if(character.level<level)continue;const selected=selections[level]??RANDOM,canGrapple=!grapplerTaken&&next.abilities.str>=13;if(![RANDOM,"asi","grappler"].includes(selected))throw new Error(`Illegal 2014 Barbarian advancement selection at level ${level}: ${selected}`);const useFeat=selected==="grappler"||(selected===RANDOM&&canGrapple&&Math.random()<.25);if(useFeat){if(!canGrapple)throw new Error(`Grappler is not legal for this 2014 Barbarian at level ${level}.`);const feat=requiredFeat(data,"grappler");next.feats.push(feat);grapplerTaken=true;choices.push({level,type:"feat",id:feat.id,name:feat.name,locked:selected!==RANDOM});}else choices.push({level,type:"asi",name:"Ability Score Improvement",increases:applyAsi(next.abilities,["str","con","dex"],20),locked:selected!==RANDOM});}
    }else if(character.ruleset==="2024"){
      let grapplerTaken=next.feats.some(feat=>feat.id==="grappler");for(const level of NORMAL_LEVELS){if(character.level<level)continue;const selected=selections[level]??RANDOM,canGrapple=!grapplerTaken&&(next.abilities.str>=13||next.abilities.dex>=13);if(![RANDOM,"ability-score-improvement","grappler"].includes(selected))throw new Error(`Illegal 2024 Barbarian General feat at level ${level}: ${selected}`);const useGrappler=selected==="grappler"||(selected===RANDOM&&canGrapple&&Math.random()<.25);if(useGrappler){if(!canGrapple)throw new Error(`Grappler is not legal for this 2024 Barbarian at level ${level}.`);const feat=requiredFeat(data,"grappler"),ability=bestAbility(next.abilities,["str","dex"],20);next.abilities[ability]+=1;next.feats.push(feat);grapplerTaken=true;choices.push({level,type:"feat",id:feat.id,name:feat.name,increases:{[ability]:1},locked:selected!==RANDOM});}else{const feat=requiredFeat(data,"ability-score-improvement"),increases=applyAsi(next.abilities,["str","con","dex"],20);next.feats.push(feat);choices.push({level,type:"feat",id:feat.id,name:feat.name,increases,locked:selected!==RANDOM});}}
      if(character.level>=19){const selected=selections[19]??RANDOM;if(selected!==RANDOM&&!EPIC_BOONS.includes(selected))throw new Error(`Illegal Barbarian Epic Boon selection: ${selected}`);const feat=requiredFeat(data,selected===RANDOM?pick(EPIC_BOONS):selected),ability=epicAbility(next.abilities,feat.id);next.abilities[ability]+=1;next.abilityMaximums[ability]=Math.max(next.abilityMaximums[ability]||20,next.abilities[ability]);next.feats.push(feat);choices.push({level:19,type:"epic-boon",id:feat.id,name:feat.name,increases:{[ability]:1},locked:selected!==RANDOM});}
    }else throw new Error(`Unsupported Barbarian advancement ruleset: ${character.ruleset}`);
    next.feats=dedupeRepeatable(next.feats);next.advancementChoices=choices;return next;
  }catch(error){console.error("[barbarian-advancement] advancement failed",error);throw error;}
}
function applyAsi(abilities,priority,max){try{const first=bestAbility(abilities,priority,max),result={},room=max-abilities[first];if(room>=2){abilities[first]+=2;result[first]=2;return result;}if(room===1){abilities[first]+=1;result[first]=1;}const remaining=priority.filter(a=>a!==first&&abilities[a]<max);if(remaining.length){const second=bestAbility(abilities,remaining,max);abilities[second]+=1;result[second]=(result[second]||0)+1;}return result;}catch(error){console.error("[barbarian-advancement] ASI failed",error);throw error;}}
function bestAbility(abilities,priority,max){const legal=priority.filter(id=>abilities[id]<max);if(!legal.length)throw new Error(`No legal ability remains below ${max}.`);return legal.sort((a,b)=>abilities[b]-abilities[a]||priority.indexOf(a)-priority.indexOf(b))[0];}
function epicAbility(abilities,featId){if(featId==="boon-irresistible-offense")return"str";return bestAbility(abilities,["str","con","dex","wis","cha","int"],30);}
function requiredFeat(data,id){const feat=data.feats.find(item=>item.id===id);if(!feat)throw new Error(`Required RAW feat is missing: ${id}`);return feat;}
function dedupeRepeatable(feats){const seen=new Set();return feats.filter(feat=>{if(feat.id==="ability-score-improvement")return true;if(seen.has(feat.id))return false;seen.add(feat.id);return true;});}
export const BARBARIAN_ADVANCEMENT=Object.freeze({normal2024:NORMAL_LEVELS,levels2014:LEVELS_2014,epicBoons:EPIC_BOONS});
